# soksak-contract-composition

Public 0.0.1 contract for settings.json, installed unit identity, installation sources,
development mode and updater policy. Dependency manifests, bindings and the resolved graph are
added as separate verified increments.
